.terms-modal-overlay {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--color-modal-bg);
    -webkit-backdrop-filter: blur(8px);
    backdrop-filter: blur(8px);
    z-index: var(--z-modal);
    justify-content: center;
    align-items: center;
    padding: var(--spacing-3); /* Reduced from spacing-5 for wider modal */
}

.terms-modal-overlay.active {
    display: flex;
}

.terms-modal-container {
    background: var(--color-white);
    width: 95%; /* Increased from 90% for wider modal */
    max-width: 1100px;
    max-height: 90vh;
    border-radius: var(--radius-2xl);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    box-shadow: var(--shadow-2xl);
}

.terms-modal-header {
    background: var(--color-primary);
    color: var(--color-white);
    padding: var(--spacing-5) var(--spacing-8);
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.terms-modal-header h2 {
    font-size: var(--font-size-4xl);
    margin: 0;
    font-weight: var(--font-weight-semibold);
    font-family: var(--font-family-display);
}

.terms-close-btn {
    background: none;
    border: none;
    color: var(--color-white);
    font-size: var(--font-size-3xl);
    cursor: pointer;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-full);
    transition: background var(--transition-base);
}

.terms-close-btn:hover {
    background: rgba(255, 255, 255, 0.1);
}

.terms-modal-body {
    display: flex;
    flex: 1;
    overflow: hidden;
}

.terms-sidebar {
    width: 280px;
    background: var(--color-text-light);
    padding: var(--spacing-8) var(--spacing-5);
    overflow-y: auto;
    border-right: 1px solid #ddd;
    box-sizing: border-box;
}

/* Mobile dropdown navigation - hidden by default */
.terms-mobile-nav {
    display: none;
    width: 100%;
    position: relative;
}

.terms-dropdown-trigger {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--spacing-3);
    padding: var(--spacing-3) var(--spacing-4);
    font-size: var(--font-size-base);
    border: 2px solid #ddd;
    border-radius: var(--radius-md);
    background: var(--color-white);
    color: var(--color-text-dark);
    cursor: pointer;
    font-weight: var(--font-weight-medium);
    transition: var(--transition-all);
}

.terms-dropdown-trigger:focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
}

.terms-dropdown-trigger[aria-expanded="true"] {
    border-color: var(--color-primary);
    box-shadow: 0 0 0 4px rgba(17, 83, 128, 0.12);
}

.terms-dropdown-label {
    flex: 1;
    text-align: left;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.terms-dropdown-icon {
    width: 1.25rem;
    height: 1.25rem;
    position: relative;
}

.terms-dropdown-icon::before,
.terms-dropdown-icon::after {
    content: '';
    position: absolute;
    left: 50%;
    top: 50%;
    width: 70%;
    height: 2px;
    background: var(--color-text-dark);
    transition: transform var(--transition-base);
}

.terms-dropdown-icon::before {
    transform: translate(-50%, -50%) rotate(45deg);
}

.terms-dropdown-icon::after {
    transform: translate(-50%, -50%) rotate(-45deg);
}

.terms-dropdown-trigger[aria-expanded="true"] .terms-dropdown-icon::before {
    transform: translate(-50%, -50%) rotate(-45deg);
}

.terms-dropdown-trigger[aria-expanded="true"] .terms-dropdown-icon::after {
    transform: translate(-50%, -50%) rotate(45deg);
}

.terms-dropdown-list {
    position: absolute;
    top: calc(100% + var(--spacing-2));
    left: 0;
    right: 0;
    width: 100%;
    background: var(--color-white);
    border: 1px solid #ddd;
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-lg, 0 16px 32px rgba(15, 23, 42, 0.18));
    max-height: 18rem;
    overflow-y: auto;
    z-index: 10;
}

.terms-dropdown-list li + li {
    border-top: 1px solid #eee;
}

.terms-dropdown-list button {
    width: 100%;
    text-align: left;
    background: transparent;
    border: none;
    padding: var(--spacing-3) var(--spacing-4);
    font-size: var(--font-size-base);
    cursor: pointer;
    color: var(--color-text-dark);
    transition: var(--transition-all);
}

.terms-dropdown-list button:hover,
.terms-dropdown-list button:focus {
    background: #f5f6f8;
}

.terms-dropdown-list button.is-active {
    font-weight: var(--font-weight-semibold);
    color: var(--color-primary);
}

/* Desktop navigation - visible by default */
.terms-desktop-nav {
    display: block;
}

.terms-desktop-nav ul {
    list-style: none;
    padding: 0;
    margin: 0;
}

.terms-desktop-nav li {
    margin-bottom: var(--spacing-4);
}

.terms-desktop-nav a {
    color: var(--color-text-muted);
    text-decoration: none;
    font-size: var(--font-size-sm);
    display: block;
    padding: var(--spacing-2) var(--spacing-3);
    border-radius: var(--radius-md);
    transition: var(--transition-all);
}

.terms-desktop-nav a:hover {
    background: #e9ecef;
    color: var(--color-text-dark);
}

.terms-desktop-nav a.active {
    font-weight: var(--font-weight-bold);
    color: var(--color-primary);
    background: #e9ecef;
}

.terms-content {
    flex: 1;
    padding: var(--spacing-10);
    overflow-y: auto;
    color: var(--color-text-dark);
}

.terms-content h3 {
    color: var(--color-primary);
    font-size: var(--font-size-2xl);
    margin-top: var(--spacing-8);
    margin-bottom: var(--spacing-4);
    padding-bottom: var(--spacing-3);
    border-bottom: 2px solid var(--color-accent);
}

.terms-content h3:first-child {
    margin-top: 0;
}

.terms-content p {
    line-height: var(--line-height-loose);
    margin-bottom: var(--spacing-5);
    text-align: justify;
}

.terms-content ul {
    margin: var(--spacing-4) 0;
    padding-left: var(--spacing-6);
}

.terms-content li {
    margin-bottom: var(--spacing-3);
    line-height: var(--line-height-loose);
}

.terms-last-updated {
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-muted);
    font-size: var(--font-size-sm);
    margin-bottom: var(--spacing-5);
}

.terms-intro {
    margin-bottom: var(--spacing-8);
    line-height: var(--line-height-loose);
}

.terms-modal-footer {
    background: var(--color-text-light);
    padding: var(--spacing-5) var(--spacing-10);
    border-top: 1px solid #ddd;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.terms-accept-btn,
.terms-decline-btn {
    padding: var(--spacing-3) var(--spacing-8);
    border: none;
    border-radius: var(--radius-3xl);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-semibold);
    cursor: pointer;
    transition: var(--transition-all);
}

.terms-accept-btn {
    background: var(--color-primary);
    color: var(--color-white);
}

.terms-accept-btn:hover {
    background: var(--color-primary-light);
}

.terms-decline-btn {
    background: transparent;
    color: var(--color-text-muted);
    border: 2px solid #ddd;
}

.terms-decline-btn:hover {
    background: var(--color-text-light);
    border-color: #999;
}

@media (max-width: 768px) {
    /* Reduce overlay padding on mobile for wider modal */
    .terms-modal-overlay {
        padding: var(--spacing-2);
    }

    .terms-modal-container {
        width: 98%; /* Even wider on mobile */
        max-height: 95vh;
    }

    .terms-modal-body {
        flex-direction: column;
    }

    .terms-sidebar {
        width: 100%;
        border-right: none;
        border-bottom: 1px solid #ddd;
        padding: var(--spacing-4) var(--spacing-5);
        overflow-y: visible;
    }

    /* Show mobile dropdown, hide desktop nav */
    .terms-mobile-nav {
        display: block;
    }

    .terms-desktop-nav {
        display: none;
    }

    .terms-content {
        padding: var(--spacing-5);
    }

    .terms-modal-header {
        padding: var(--spacing-4) var(--spacing-5);
    }

    .terms-modal-header h2 {
        font-size: var(--font-size-2xl);
    }

    .terms-modal-footer {
        flex-direction: column;
        gap: var(--spacing-3);
        padding: var(--spacing-4) var(--spacing-5);
    }

    .terms-accept-btn,
    .terms-decline-btn {
        width: 100%;
    }
}
