# Environment variables (NEVER commit these!)
.env
.env.local
.env.*.local

# Sensitive credentials (contains passwords!)
app.yaml

# PHP dependencies (regenerated via composer install)
vendor/

# Node modules
node_modules/
server-renderer/node_modules/
server-renderer/.cache/

# Node.js dependencies (install via npm install in server-renderer/)
server-renderer/node_modules/
