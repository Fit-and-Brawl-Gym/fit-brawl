# Fit and Brawl Gym - Database Entity Relationship Diagram

## Database Overview
**Database Name:** `fit_and_brawl_gym`  
**Generated:** November 19, 2025  
**Total Tables:** 30

---

## Core Entities

### 👤 Users System

```
┌─────────────────────────────────────────────────────────────┐
│ users                                                        │
├─────────────────────────────────────────────────────────────┤
│ PK  id (VARCHAR(15))                                        │
│ UQ  username (VARCHAR(50))                                  │
│ UQ  email (VARCHAR(100))                                    │
│     email_encrypted (TEXT)                                  │
│     contact_number (VARCHAR(20))                            │
│     password (VARCHAR(255))                                 │
│     role (ENUM: member, admin, trainer)                     │
│     avatar (VARCHAR(255))                                   │
│     is_verified (TINYINT)                                   │
│     account_status (ENUM: active, suspended, locked,        │
│                           pending)                          │
│     verification_token (VARCHAR(255))                       │
│     otp (VARCHAR(6))                                        │
│     otp_expiry (DATETIME)                                   │
│     otp_attempts (INT)                                      │
│     last_otp_request (TIMESTAMP)                            │
│     last_logout (DATETIME)                                  │
│     created_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🥊 Membership & Subscription System

### Memberships

```
┌─────────────────────────────────────────────────────────────┐
│ memberships                                                  │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│     plan_name (VARCHAR(50))                                 │
│     class_type (VARCHAR(100))                               │
│     weekly_hours_limit (INT) DEFAULT 24                     │
└─────────────────────────────────────────────────────────────┘
       │
       │ M:N
       ▼
┌─────────────────────────────────────────────────────────────┐
│ membership_trainers (Junction Table)                        │
├─────────────────────────────────────────────────────────────┤
│ PK  membership_id (INT) ────────────┐                      │
│ PK  trainer_id (INT)                │                      │
└─────────────────────────────────────┼──────────────────────┘
                                      │
                                      ▼
                              ┌──────────────┐
                              │   trainers   │
                              └──────────────┘
```

### User Memberships

```
┌─────────────────────────────────────────────────────────────┐
│ user_memberships                                             │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  user_id (VARCHAR(15)) ──────────► users.id             │
│ FK  plan_id (INT) ──────────────────► memberships.id       │
│ FK  admin_id (VARCHAR(15)) ─────────► users.id             │
│ FK  cash_received_by (VARCHAR(15)) ─► users.id             │
│     name (VARCHAR(255))                                     │
│     country (VARCHAR(100))                                  │
│     permanent_address (VARCHAR(255))                        │
│     plan_name (VARCHAR(100))                                │
│     duration (INT) - days                                   │
│     qr_proof (VARCHAR(255))                                 │
│     date_submitted (DATETIME)                               │
│     date_approved (DATETIME)                                │
│     remarks (VARCHAR(255))                                  │
│     request_status (ENUM: pending, approved, rejected)      │
│     start_date (DATE)                                       │
│     end_date (DATE)                                         │
│     billing_type (ENUM: monthly, quarterly)                 │
│     payment_method (ENUM: online, cash)                     │
│     cash_payment_status (ENUM: unpaid, paid, cancelled)     │
│     cash_payment_date (DATETIME)                            │
│     membership_status (ENUM: active, expired, cancelled)    │
│     source_table (ENUM: user_memberships, subscriptions)    │
│     source_id (INT)                                         │
│     created_at (TIMESTAMP)                                  │
│     updated_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 👨‍🏫 Trainer Management System

### Trainers

```
┌─────────────────────────────────────────────────────────────┐
│ trainers                                                     │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  user_id (VARCHAR(15)) ──────────► users.id             │
│ UQ  email (VARCHAR(100))                                    │
│     name (VARCHAR(100))                                     │
│     phone (VARCHAR(20))                                     │
│     specialization (ENUM: Gym, MMA, Boxing, Muay Thai)      │
│     bio (TEXT)                                              │
│     photo (VARCHAR(255))                                    │
│     emergency_contact_name (VARCHAR(100))                   │
│     emergency_contact_phone (VARCHAR(20))                   │
│     max_clients_per_day (INT) DEFAULT 3                     │
│     status (ENUM: Active, Inactive, On Leave)               │
│     password_changed (TINYINT)                              │
│     deleted_at (TIMESTAMP)                                  │
│     created_at (TIMESTAMP)                                  │
│     updated_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
       │
       ├──────────────────────────────────────────────┐
       │                                               │
       ▼                                               ▼
┌──────────────────────────┐              ┌──────────────────────────┐
│ trainer_shifts           │              │ trainer_day_offs         │
├──────────────────────────┤              ├──────────────────────────┤
│ PK  id (INT)            │              │ PK  id (INT)            │
│ FK  trainer_id (INT)    │              │ FK  trainer_id (INT)    │
│ UQ  trainer_id +        │              │ UQ  trainer_id +        │
│     day_of_week         │              │     day_of_week         │
│     day_of_week (ENUM)  │              │     day_of_week (ENUM)  │
│     shift_type (ENUM:   │              │     is_day_off (BOOL)   │
│       morning,          │              │     created_at          │
│       afternoon,        │              │     updated_at          │
│       night, none)      │              └──────────────────────────┘
│     custom_start_time   │
│     custom_end_time     │              ┌──────────────────────────┐
│     break_start_time    │              │ trainer_availability_    │
│     break_end_time      │              │ blocks                   │
│     is_active (BOOL)    │              ├──────────────────────────┤
│     created_at          │              │ PK  id (INT)            │
│     updated_at          │              │ FK  trainer_id (INT)    │
└──────────────────────────┘              │ FK  blocked_by          │
       │                                   │     (VARCHAR(15))       │
       │                                   │     date (DATE)         │
       │                                   │     block_start_time    │
       │                                   │     block_end_time      │
       ▼                                   │     is_all_day (BOOL)   │
┌──────────────────────────┐              │     session_time (ENUM) │
│ trainer_activity_log     │              │     reason (VARCHAR)    │
├──────────────────────────┤              │     block_status (ENUM) │
│ PK  id (INT)            │              │     created_at          │
│ FK  trainer_id (INT)    │              │     updated_at          │
│ FK  admin_id (VARCHAR)  │              └──────────────────────────┘
│     action (VARCHAR)    │
│     details (TEXT)      │
│     timestamp (DATETIME)│
└──────────────────────────┘
```

---

## 📅 Booking & Reservation System

### User Reservations

```
┌─────────────────────────────────────────────────────────────┐
│ user_reservations                                            │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  user_id (VARCHAR(15)) ──────────► users.id             │
│ FK  trainer_id (INT) ────────────────► trainers.id         │
│ FK  recurring_parent_id (INT) ──────► recurring_bookings.id│
│ FK  rescheduled_from_id (INT) ──────► user_reservations.id │
│     session_time (ENUM: Morning, Afternoon, Evening)        │
│     class_type (ENUM: Boxing, Muay Thai, MMA, Gym)          │
│     booking_date (DATE)                                     │
│     start_time (DATETIME)                                   │
│     end_time (DATETIME)                                     │
│     reschedule_reason (VARCHAR(255))                        │
│     rescheduled_at (TIMESTAMP)                              │
│     buffer_minutes (INT) DEFAULT 10                         │
│     booking_status (ENUM: confirmed, completed, cancelled,  │
│                           blocked)                          │
│     booked_at (TIMESTAMP)                                   │
│     updated_at (TIMESTAMP)                                  │
│     reservation_state (VARCHAR(50))                         │
│     cancelled_at (TIMESTAMP)                                │
│     unavailable_marked_at (DATETIME)                        │
└─────────────────────────────────────────────────────────────┘
       ▲
       │
       │
┌─────────────────────────────────────────────────────────────┐
│ recurring_bookings                                           │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  user_id (VARCHAR(15)) ──────────► users.id             │
│ FK  trainer_id (INT) ────────────────► trainers.id         │
│     class_type (ENUM: Boxing, Muay Thai, MMA, Gym)          │
│     recurrence_pattern (ENUM: weekly)                       │
│     start_date (DATE)                                       │
│     end_date (DATE)                                         │
│     start_time (TIME)                                       │
│     end_time (TIME)                                         │
│     occurrences_count (INT)                                 │
│     status (ENUM: active, cancelled, completed)             │
│     created_at (TIMESTAMP)                                  │
│     updated_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

### Booking Configuration

```
┌─────────────────────────────────────────────────────────────┐
│ booking_config                                               │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ UQ  config_key (VARCHAR(50))                                │
│     config_value (VARCHAR(255))                             │
│     description (TEXT)                                      │
│     updated_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏋️ Gym Resources

### Equipment Management

```
┌─────────────────────────────────────────────────────────────┐
│ equipment                                                    │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│     name (VARCHAR(100))                                     │
│     category (ENUM: Cardio, Flexibility, Core,              │
│                     Strength Training,                      │
│                     Functional Training)                    │
│     status (ENUM: Available, Maintenance, Out of Order)     │
│     maintenance_start_date (DATE)                           │
│     maintenance_end_date (DATE)                             │
│     maintenance_reason (TEXT)                               │
│     description (TEXT)                                      │
│     image_path (VARCHAR(255))                               │
│     created_at (TIMESTAMP)                                  │
│     updated_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

### Products

```
┌─────────────────────────────────────────────────────────────┐
│ products                                                     │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│     name (VARCHAR(100))                                     │
│     category (ENUM: Supplements, Hydration & Drinks,        │
│                     Snacks, Accessories,                    │
│                     Boxing & Muay Thai Products)            │
│     stock (INT)                                             │
│     status (ENUM: in stock, low stock, out of stock)        │
│     image_path (VARCHAR(255))                               │
│     created_at (TIMESTAMP)                                  │
│     updated_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 💬 Communication System

### Contact Messages

```
┌─────────────────────────────────────────────────────────────┐
│ contact                                                      │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│     first_name (VARCHAR(50))                                │
│     last_name (VARCHAR(50))                                 │
│     email (VARCHAR(100))                                    │
│     phone_number (VARCHAR(20))                              │
│     message (TEXT)                                          │
│     status (ENUM: unread, read)                             │
│     archived (TINYINT)                                      │
│     deleted_at (TIMESTAMP)                                  │
│     date_submitted (TIMESTAMP)                              │
└─────────────────────────────────────────────────────────────┘
```

### Feedback System

```
┌─────────────────────────────────────────────────────────────┐
│ feedback                                                     │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  user_id (VARCHAR(15)) ──────────► users.id             │
│     username (VARCHAR(50))                                  │
│     email (VARCHAR(100))                                    │
│     avatar (VARCHAR(255))                                   │
│     message (TEXT)                                          │
│     is_visible (TINYINT)                                    │
│     helpful_count (INT)                                     │
│     not_helpful_count (INT)                                 │
│     date (TIMESTAMP)                                        │
└─────────────────────────────────────────────────────────────┘
       │
       │ 1:N
       ▼
┌─────────────────────────────────────────────────────────────┐
│ feedback_votes                                               │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  feedback_id (INT) ───────────────► feedback.id         │
│ FK  user_id (VARCHAR(15)) ───────────► users.id            │
│ UQ  feedback_id + user_id                                   │
│     vote_type (ENUM: helpful, not_helpful)                  │
│     created_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

### User Notifications

```
┌─────────────────────────────────────────────────────────────┐
│ user_notifications                                           │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  user_id (VARCHAR(15)) ──────────► users.id             │
│     notification_type (VARCHAR(50))                         │
│     title (VARCHAR(255))                                    │
│     message (TEXT)                                          │
│     admin_identifier (VARCHAR(100))                         │
│     is_read (TINYINT)                                       │
│     sent_via_email (TINYINT)                                │
│     created_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 Security & Authentication

### Session Management

```
┌─────────────────────────────────────────────────────────────┐
│ active_sessions                                              │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  user_id (VARCHAR(50)) ───────────► users.id            │
│ IDX session_id (VARCHAR(128))                               │
│     ip_address (VARCHAR(45))                                │
│     user_agent (TEXT)                                       │
│     login_time (TIMESTAMP)                                  │
│     last_activity (TIMESTAMP)                               │
│     is_current (TINYINT)                                    │
└─────────────────────────────────────────────────────────────┘
```

### Password Management

```
┌─────────────────────────────────────────────────────────────┐
│ password_reset_tokens                                        │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  user_id (VARCHAR(15)) ───────────► users.id            │
│ FK  created_by (VARCHAR(15)) ────────► users.id (admin)    │
│ UQ  token (VARCHAR(255))                                    │
│     expires_at (DATETIME)                                   │
│     used_at (DATETIME)                                      │
│     ip_address (VARCHAR(45))                                │
│     user_agent (VARCHAR(255))                               │
│     created_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ remember_password                                            │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  user_id (VARCHAR(15)) ───────────► users.id            │
│     token_hash (VARCHAR(255))                               │
│     created_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

### Security Monitoring

```
┌─────────────────────────────────────────────────────────────┐
│ login_attempts                                               │
├─────────────────────────────────────────────────────────────┤
│ PK  identifier (VARCHAR(255))                               │
│     attempt_count (INT)                                     │
│     last_attempt (DATETIME)                                 │
│     created_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ security_events                                              │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│     event_type (VARCHAR(50))                                │
│     severity (ENUM: low, medium, high, critical)            │
│     user_id (VARCHAR(50))                                   │
│     username (VARCHAR(100))                                 │
│     ip_address (VARCHAR(45))                                │
│     user_agent (TEXT)                                       │
│     endpoint (VARCHAR(255))                                 │
│     details (TEXT)                                          │
│     created_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ api_rate_limits                                              │
├─────────────────────────────────────────────────────────────┤
│ PK  identifier (VARCHAR(255))                               │
│     request_count (INT)                                     │
│     window_start (DATETIME)                                 │
│     updated_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

### Sensitive Changes

```
┌─────────────────────────────────────────────────────────────┐
│ sensitive_change_requests                                    │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  user_id (VARCHAR(15)) ───────────► users.id            │
│ FK  admin_id (VARCHAR(15)) ──────────► users.id (admin)    │
│ UQ  confirmation_token (VARCHAR(255))                       │
│     change_type (ENUM: email, phone, recovery_email,        │
│                        security_question)                   │
│     old_value (VARCHAR(255))                                │
│     new_value (VARCHAR(255))                                │
│     status (ENUM: pending, confirmed, rejected, expired)    │
│     expires_at (DATETIME)                                   │
│     confirmed_at (DATETIME)                                 │
│     ip_address (VARCHAR(45))                                │
│     user_agent (VARCHAR(255))                               │
│     created_at (TIMESTAMP)                                  │
│     updated_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ security_verification_codes                                  │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  admin_id (VARCHAR(15)) ──────────► users.id            │
│ UQ  code (VARCHAR(64))                                      │
│     purpose (VARCHAR(100))                                  │
│     valid_until (DATETIME)                                  │
│     used_at (DATETIME)                                      │
│     created_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 👮 Admin System

### Admin Permissions

```
┌─────────────────────────────────────────────────────────────┐
│ admin_permissions                                            │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  admin_id (VARCHAR(15)) ──────────► users.id            │
│ FK  granted_by (VARCHAR(15)) ────────► users.id            │
│ UQ  admin_id + permission_name                              │
│     permission_name (VARCHAR(100))                          │
│     granted_at (TIMESTAMP)                                  │
│     expires_at (DATETIME)                                   │
│     is_active (TINYINT)                                     │
└─────────────────────────────────────────────────────────────┘
```

### Admin Logs

```
┌─────────────────────────────────────────────────────────────┐
│ admin_logs                                                   │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ FK  admin_id (VARCHAR(15)) ──────────► users.id            │
│ FK  target_user_id (VARCHAR(15)) ────► users.id            │
│     admin_name (VARCHAR(100))                               │
│     action_type (VARCHAR(50))                               │
│     target_user (VARCHAR(100))                              │
│     target_id (INT)                                         │
│     details (TEXT)                                          │
│     previous_value (TEXT)                                   │
│     new_value (TEXT)                                        │
│     ip_address (VARCHAR(45))                                │
│     user_agent (VARCHAR(255))                               │
│     severity (ENUM: low, medium, high, critical)            │
│     timestamp (DATETIME)                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Logging System

### Unified Logs

```
┌─────────────────────────────────────────────────────────────┐
│ unified_logs                                                 │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│     log_level (ENUM: debug, info, warning, error, critical) │
│     log_source (ENUM: security, activity, application,      │
│                       database, email, system)              │
│     category (VARCHAR(100))                                 │
│     message (TEXT)                                          │
│     user_id (VARCHAR(50))                                   │
│     username (VARCHAR(100))                                 │
│     ip_address (VARCHAR(45))                                │
│     endpoint (VARCHAR(255))                                 │
│     context (LONGTEXT JSON)                                 │
│     stack_trace (TEXT)                                      │
│     created_at (TIMESTAMP)                                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ activity_log                                                 │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│     user_id (VARCHAR(15))                                   │
│     role (ENUM: member, trainer, admin)                     │
│     action (VARCHAR(50))                                    │
│     timestamp (DATETIME)                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 💰 Non-Member Transactions

```
┌─────────────────────────────────────────────────────────────┐
│ nonmember_receipts                                           │
├─────────────────────────────────────────────────────────────┤
│ PK  id (INT)                                                │
│ UQ  receipt_id (VARCHAR(32))                                │
│     service (VARCHAR(64))                                   │
│     name (VARCHAR(128))                                     │
│     email (VARCHAR(128))                                    │
│     phone (VARCHAR(32))                                     │
│     service_date (DATE)                                     │
│     amount (INT)                                            │
│     created_at (DATETIME)                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔗 Key Relationships Summary

### One-to-Many (1:N)
- `users` → `user_memberships` (user_id)
- `users` → `user_reservations` (user_id)
- `users` → `feedback` (user_id)
- `users` → `user_notifications` (user_id)
- `users` → `active_sessions` (user_id)
- `trainers` → `user_reservations` (trainer_id)
- `trainers` → `trainer_shifts` (trainer_id)
- `trainers` → `trainer_day_offs` (trainer_id)
- `trainers` → `trainer_availability_blocks` (trainer_id)
- `trainers` → `recurring_bookings` (trainer_id)
- `memberships` → `user_memberships` (plan_id)
- `feedback` → `feedback_votes` (feedback_id)
- `recurring_bookings` → `user_reservations` (recurring_parent_id)

### Many-to-Many (M:N)
- `memberships` ↔ `trainers` (through `membership_trainers`)

### Self-Referential
- `user_reservations` → `user_reservations` (rescheduled_from_id)

### Admin/Audit Relations
- `users` (admin) → `user_memberships` (admin_id, cash_received_by)
- `users` (admin) → `trainer_activity_log` (admin_id)
- `users` (admin) → `trainer_availability_blocks` (blocked_by)
- `users` (admin) → `admin_logs` (admin_id, target_user_id)
- `users` (admin) → `admin_permissions` (admin_id, granted_by)
- `users` (admin) → `password_reset_tokens` (created_by)
- `users` (admin) → `sensitive_change_requests` (admin_id)
- `users` (admin) → `security_verification_codes` (admin_id)

---

## 📝 Notes

### ID Format Conventions
- **Users:** `MBR-25-0012` (Members), `TRN-25-0003` (Trainers), `ADM-25-0001` (Admins)
- **Trainers:** Auto-increment INT
- **Reservations:** Auto-increment INT
- **Receipts:** 32-character hash

### Timestamp Fields
- Most tables use `created_at` (TIMESTAMP DEFAULT CURRENT_TIMESTAMP)
- Many tables include `updated_at` (ON UPDATE CURRENT_TIMESTAMP)
- Soft deletes use `deleted_at` (TIMESTAMP NULL)

### Enum Values
Common enums across tables:
- **Roles:** member, trainer, admin
- **Status:** active, inactive, pending, suspended, locked
- **Days:** Monday-Sunday
- **Sessions:** Morning, Afternoon, Evening
- **Training Types:** Boxing, Muay Thai, MMA, Gym
- **Severity:** low, medium, high, critical

### Security Features
- Password hashing (bcrypt)
- Token-based authentication
- Rate limiting
- Login attempt tracking
- Security event logging
- Two-factor authentication (OTP)
- Session management
- API security middleware

### Booking System Features
- Session-based bookings (Morning/Afternoon/Evening)
- Recurring weekly bookings
- Trainer shift management
- Day-off scheduling
- Availability blocking
- Rescheduling with history
- Buffer time management
- Multiple training disciplines

### Payment System
- Dual payment methods (online/cash)
- Cash payment tracking
- Admin approval workflow
- Billing cycles (monthly/quarterly)
- QR proof upload
- Payment status tracking

---

**Generated from:** `schema.sql`  
**Database Version:** MariaDB 10.4.32  
**PHP Version:** 8.3.27
