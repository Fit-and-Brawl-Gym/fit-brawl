# This file specifies files that are *not* uploaded to Google Cloud Platform
# using gcloud. It follows the same syntax as .gitignore, with the addition of
# "#!include" directives (which insert the entries of the given .gitignore-style
# file at that point).

# Include .gitignore if it exists
#!include .gitignore

# Git files
.git/
.gitignore
.github/

# Environment and config files
.env
.env.local
.env.*.local

# Node.js
node_modules/
server-renderer/node_modules/
server-renderer/.cache/
server-renderer/package-lock.json

# PHP dependencies (vendor will be reinstalled on GCP)
# NOTE: Comment this out if you want to include vendor directory
# vendor/

# Development files
.vscode/
.idea/
*.log
*.bak
*.tmp
*~

# Documentation
README.md
DEPLOYMENT_GCP.md
docs/
root-project-structure.ini

# Local uploads (these will be moved to Cloud Storage)
uploads/avatars/*
uploads/equipment/*
uploads/products/*
uploads/receipts/*
# Keep the directory structure
!uploads/avatars/.gitkeep
!uploads/equipment/.gitkeep
!uploads/products/.gitkeep
!uploads/receipts/.gitkeep

# Database files
*.sql
*.sqlite
*.db

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Testing and development
tests/
*.test.php
phpunit.xml

# Build artifacts
dist/
build/

# Composer lock (optional, keep if you want consistent versions)
# composer.lock
